import SectionWrapper from "../../../../Components/SectionWrapper";

const CategoryDiv = () => {
  return <SectionWrapper className="bg-red-200">CategoryDiv</SectionWrapper>;
};

export default CategoryDiv;
